<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ogau"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 相册图片选择模块 -->
        <activity  android:name="com.ogau.photopicker.TestPhotoPickerActivity"/>
        <activity  android:name="com.ogau.photopicker.PhotoPickerActivity"/>
        <activity  android:name="com.ogau.photopicker.PhotoPreviewActivity"/>
        <!-- 图片压缩 -->
        <activity  android:name="com.ogau.photocompress.PhotoCompressTestActivity"/>
        <!--  -->
    </application>

</manifest>
